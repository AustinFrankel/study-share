# Ignore everything at root by default
*

# Keep Vercel config and root package.json
!vercel.json
!package.json

# Ensure the app workspace is included (directory and all contents)
!study-resources
!study-resources/**
